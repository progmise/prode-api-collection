info:
  name: Delete usuario by id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{protocol}}://{{host}}{{path_api}}/usuarios/{{usuario_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
