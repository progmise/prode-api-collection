info:
  name: Get usuario by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{host}}{{path_api}}/usuarios/{{usuario_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
