info:
  name: Get partido by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{protocol}}://{{host}}{{path_api}}/partidos/{{partido_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
