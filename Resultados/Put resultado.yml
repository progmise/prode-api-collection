info:
  name: Put resultado
  type: http
  seq: 5

http:
  method: PUT
  url: "{{protocol}}://{{host}}{{path_api}}/partidos/{{partido_id}}/resultado"
  body:
    type: json
    data: |-
      {
        "local": 0,
        "visitante": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
